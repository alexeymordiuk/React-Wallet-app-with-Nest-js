import styled from 'styled-components';

export const HeaderWrraper = styled.header({
    padding: '10px 20px',
    marginTop: 'auto',
    borderTop: '1px solid #40513b',
    width: '100%',
})

export const Links = styled.ul({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: '0 20px'
})

export const Nav = styled.nav({
})
